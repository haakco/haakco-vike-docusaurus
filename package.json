{
  "name": "@haakco/vike-plugin-docusaurus",
  "version": "0.1.0",
  "description": "Vike plugin and build helpers for serving a Docusaurus site with Pagefind",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/haakco/haakco-vike-docusaurus.git"
  },
  "homepage": "https://github.com/haakco/haakco-vike-docusaurus",
  "bugs": {
    "url": "https://github.com/haakco/haakco-vike-docusaurus/issues"
  },
  "packageManager": "pnpm@10.32.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./browser": {
      "types": "./dist/root.d.ts",
      "import": "./dist/root.js"
    }
  },
  "bin": {
    "haakco-vike-docusaurus-build": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "pnpm run build:clean && pnpm exec tsc -p tsconfig.json --types node",
    "build:clean": "node -e \"import('node:fs/promises').then((fs) => fs.rm('dist', { recursive: true, force: true }))\"",
    "typecheck": "pnpm exec tsc -p tsconfig.json --noEmit --types node",
    "lint": "pnpm run lint:biome && pnpm run lint:complexity && pnpm run lint:dup && pnpm run typecheck",
    "lint:biome": "pnpm exec biome check . --colors=off",
    "lint:ci": "pnpm run lint",
    "lint:complexity": "pnpm exec biome lint src --colors=off --only=lint/complexity/noExcessiveCognitiveComplexity",
    "lint:dup": "pnpm exec jscpd --config jscpd.config.json",
    "lint:fix": "pnpm exec biome check . --write --unsafe --colors=off",
    "test": "pnpm exec vitest run --config vitest.config.ts",
    "test:ci": "pnpm exec vitest run --config vitest.config.ts --coverage",
    "verify": "pnpm run lint && pnpm run test:ci && pnpm run build"
  },
  "keywords": [
    "vike",
    "vite",
    "docusaurus",
    "pagefind",
    "haakco"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pagefind": "^1.4.0"
  },
  "peerDependencies": {
    "vite": "^7.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@types/node": "^24.3.0",
    "@vitest/coverage-v8": "^3.2.4",
    "jscpd": "^4.0.5",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
